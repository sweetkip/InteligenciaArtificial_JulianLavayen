using System.Collections.Generic;
using UnityEngine;
//<>
public class SandygastNodeManager : MonoBehaviour
{
    //[SerializeField] private List<Node> nodes = new();

    //public Node GetRandomNode(Node current)
    //{
    //    if (nodes.Count <= 1)
    //        return current;

    //    Node random;

    //    do
    //    {
    //        random = nodes[Random.Range(0, nodes.Count)];
    //    }
    //    while (random == current);

    //    return random;
    //}
}
